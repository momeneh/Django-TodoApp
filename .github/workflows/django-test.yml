name: Django project test
on:
  push:
    branches: [ "api" ]
  pull_request:
    branches: [ "api" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image test output
      run: True
